#include <stdio.h>

void main()
{
	int password = 1234;
	int nhap_pass = 0;
	int i = 0;
	int tien = 0;
	printf("moi nhap pass:");
	scanf_s("%d", &nhap_pass);
	if (nhap_pass != password)
	{
			printf("moi ban nhap lai: ");
			i++;
	}
	else if (nhap_pass == password)
	{
		if(tien>=10000)
	}
}
